from dataclasses import dataclass
from pathlib import Path


def load_config(path: str | Path) -> None:
    """Placeholder for configuration loader."""
    # TODO: parse configuration file
    return None
